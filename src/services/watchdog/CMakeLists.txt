if(PICO_BUILD)
    add_library(watchdog_hal_lib watchdog_hal_pico.c)
    target_include_directories(watchdog_hal_lib PUBLIC ${CMAKE_SOURCE_DIR}/include)
    target_link_libraries(watchdog_hal_lib PUBLIC hardware_watchdog pico_stdlib)
else()
    add_library(watchdog_hal_lib watchdog_hal_host.c)
    target_include_directories(watchdog_hal_lib PUBLIC ${CMAKE_SOURCE_DIR}/include)
endif()
